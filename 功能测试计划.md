# 供应链管理系统 — 功能测试计划

## 一、测试环境准备

| 项目 | 配置 |
|------|------|
| 后端 | `localhost:8088`，MySQL `supply_chain_db`（root/root） |
| 前端 | `npm run dev` 默认 `localhost:5173` |
| IPFS | 本地 Kubo 节点 `127.0.0.1:5001`（需启动） |
| FISCO BCOS | VM 节点已启动，合约已部署 `0xe083bed62cb5fb49a844dbbc17b1cd0539d38700` |

---

## 二、测试账号矩阵

需提前注册以下 7 个账号（测试流程中包含注册步骤）：

| 序号 | 用户名 | 角色 | 用途 |
|------|--------|------|------|
| 0 | admin | admin | 系统自带，密码 `admin123` |
| 1 | supplier01 | supplier | 供应商 |
| 2 | mfg01 | manufacturer | 制造商 |
| 3 | asm01 | assembler | 组装商 |
| 4 | dist01 | distributor | 分销商 |
| 5 | user01 | enduser | 终端用户 |
| 6 | reg01 | regulator | 监管机构 |

---

## 三、测试用例

### 阶段 0：系统基础 & 用户注册

| 编号 | 功能点 | 操作步骤 | 预期结果 |
|------|--------|---------|---------|
| T001 | 管理员登录 | 打开 `/login`，输入 `admin / admin123`，点击登录 | 跳转到 `/dashboard`，显示"系统管理员"标签，4 张统计卡片有数据 |
| T002 | 供应商注册 | 点击"注册"，填写企业名/信用代码/联系人/手机，角色选"供应商"，上传营业执照+资质证书，提交 | 提示注册成功，跳转登录页；数据库 `sys_user` 新增记录，`blockchain_addr` 为真实 FISCO 地址（0x 开头 42 位），`sys_supplier_audit` 状态为 `PENDING`，有 `tx_hash` |
| T003 | 制造商注册 | 同上，角色选"制造商" | 注册成功，`blockchain_addr` 为真实链上地址 |
| T004 | 组装商注册 | 角色选"组装商" | 同上 |
| T005 | 分销商注册 | 角色选"分销商" | 同上 |
| T006 | 终端用户注册 | 角色选"终端用户" | 同上 |
| T007 | 监管注册 | 角色选"监管机构" | 同上 |
| T008 | 重复用户名 | 用已存在的用户名注册 | 提示"用户名已存在" |
| T009 | 侧边栏权限 | 分别用 7 个账号登录，检查侧边栏菜单 | supplier 只看到"供应商管理"和"首页"；manufacturer 只看到"制造商管理"和"首页"；以此类推；admin 看到全部菜单 |
| T010 | Dashboard 卡片 | 用各角色登录后查看首页 | 供应商看到"生产订单/完成率/设计文档/在途订单"；制造商看到"生产批次/设备总数/质检通过率/上链率"等 |

---

### 阶段 1：监管 — 供应商资质审核

| 编号 | 功能点 | 操作步骤 | 预期结果 |
|------|--------|---------|---------|
| T101 | 查看待审核列表 | 用 `reg01` 登录 → 监管控制台 → 资质审核 | 列表显示 supplier01 的审核记录，状态"待审核" |
| T102 | 审批通过 | 点击"通过" | 状态变"已通过"，数据库 `sys_supplier_audit.audit_status = APPROVED`，有新 `tx_hash`（链上锚定） |
| T103 | 审批驳回（备选） | 对另一个供应商点击"驳回"，填写意见 | 状态变"已驳回"，`reject_reason` 有值 |

---

### 阶段 2：供应商 — 设计文档 & BOM & 生产订单

| 编号 | 功能点 | 操作步骤 | 预期结果 |
|------|--------|---------|---------|
| T201 | 上传设计文档 | `supplier01` 登录 → 设计文档 → 上传，选择一个 PDF/图片 | 列表新增一行，显示文件哈希、IPFS CID、链上状态，`tx_hash` 非空 |
| T202 | 验证设计文档 | 点击行操作"验证" | 提示"哈希一致"或验证通过 |
| T203 | 创建 BOM | BOM 管理 → 新建，选择设计文档，添加 2-3 行物料明细，提交 | 列表新增 BOM，状态正常 |
| T204 | Excel 导入 BOM | 下载模板 → 填写物料 → 上传解析 | 自动填充到 BOM 明细表 |
| T205 | 发布生产订单 | 生产订单 → 发布，选择 BOM，数量填 10，期望交期填一个月后，可选指定制造商 mfg01 | 订单创建成功，状态"待接单"，数据库 `bus_production_request.tx_hash` 非空 |
| T206 | 未审核供应商发单 | 用未通过审核的供应商账号尝试发布订单 | 提示"供应商资质未审核通过" |
| T207 | 进度跟踪 | 进度跟踪页 → 查看刚创建的订单 → 展开行 | 时间线显示"待接单"为当前步骤，点"加载详情"能加载数据 |

---

### 阶段 3：制造商 — 接单 & 生产 & 质检

| 编号 | 功能点 | 操作步骤 | 预期结果 |
|------|--------|---------|---------|
| T301 | 订单大厅 | `mfg01` 登录 → 订单接收 → 切换到"订单大厅" | 列表显示 supplier01 发布的订单 |
| T302 | 接单 | 点击"接单"，填写承诺价格/交期，可上传协议文件 | 订单状态变为"已接单"，`bus_manufacturing_agreement` 新增记录 |
| T303 | 查看协议 | 切换到"我的订单"，点击"查看协议" | 弹窗显示协议信息 |
| T304 | 创建生产批次 | 生产管理 → 创建批次，选择订单，计划数量填 10 | 批次创建成功，`bus_production_batch.tx_hash` 非空，订单状态变"生产中" |
| T305 | 生成 ECID | 选择批次 → 生成 ECID，设备类型填"CPU"，数量 10 | ECID 管理列表新增 10 条记录，格式如 `ECID-M0001-20260331-000001` |
| T306 | 上传质检报告 | 质检管理 → 选择"按批次"，输入批次号，上传质检报告文件，选"通过" | 报告上传成功，质检报告列表新增记录 |
| T307 | 标记质检通过 | 选"通过"后点击"确认标记合格并上链" | 设备状态变 `QC_PASS`，链上有 `MFG_QC_PASS` 锚定 |
| T308 | 未上传报告直接通过 | 不选文件直接点"通过" | 提示"请先上传质检报告文件" |
| T309 | 质检不合格 | 选一个 ECID → 质检不合格，填写原因 | 设备状态变 `REJECTED`，`bus_reject_record` 新增，`tx_hash` 非空 |
| T310 | ECID 注册上链 | 选择 QC_PASS 的设备 → 注册上链 | `chain_registered = 1`，`tx_hash` 变为真实链上哈希 |
| T311 | 完成批次 | 所有 ECID 都上链通过后 → 完成批次 | 批次状态 `COMPLETED`；若所有批次完成，订单自动变 `COMPLETED` |
| T312 | CSV 导出 | 点击 ECID 导出 | 下载 CSV 文件，包含所有 ECID 信息 |
| T313 | 制造商数据看板 | 数据看板页面 | 4 张卡片数据正确，柱状图/饼图有数据，ECID 查询功能正常 |

---

### 阶段 4：组装商 — 入库 & 组装 & 质检

| 编号 | 功能点 | 操作步骤 | 预期结果 |
|------|--------|---------|---------|
| T401 | 部件入库扫描 | `asm01` 登录 → 部件入库 → 输入一个已注册上链的 ECID → 扫描 | 显示校验通过（绿色），设备信息、制造商、批次等 |
| T402 | 扫描无效 ECID | 输入一个不存在的 ECID | 提示"ECID 不存在"或校验失败 |
| T403 | Excel 批量校验 | 下载模板 → 填入多个 ECID → 上传校验 | 逐条显示校验结果 |
| T404 | 创建组装批次 | 组装管理 → 批次标签 → 新建批次，填写产品型号，计划数量 5 | 列表新增批次，`bus_assembly_batch.tx_hash` 非空 |
| T405 | 创建组装记录 | 记录标签 → 新建，选择批次，输入 2-3 个已通过的 ECID，填固件版本 | 组装记录创建，SN 自动生成（如 `SN-20260331-123456`），`assembly_tx_hash` 非空 |
| T406 | 重复 ECID 校验 | 输入已被绑定的 ECID | 提示校验未通过 |
| T407 | 组装上链注册 | 对刚创建的记录点击"上链注册" | `chain_registered = 1`，`tx_hash` 非空，状态变 `ON_CHAIN` |
| T408 | 整机质检报告 | 质检管理 → 选择 SN → 上传测试报告 → 提交 | 质检记录新增，`test_report_hash` 和 `test_report_cid` 有值 |
| T409 | 组装商数据看板 | 进入数据看板 | KPI 卡片数据正确，SN 树查询能展示 ECID 部件树 |
| T410 | CSV 导出 | 导出组装记录 | CSV 文件正确 |

---

### 阶段 5：分销商 — 物流 & 库存 & 销售

| 编号 | 功能点 | 操作步骤 | 预期结果 |
|------|--------|---------|---------|
| T501 | 发货 | `dist01` 登录 → 物流流转 → 发货，输入 SN（来自阶段 4 的已上链 SN），填写收货方 ID（可选 dist01 自己或另一个分销商），填物流公司、运单号 | 发货成功，`bus_transfer_event.tx_hash` 非空，组装记录状态变 `IN_TRANSIT` |
| T502 | 无权发货 | 用不是当前货权方的账号尝试发同一个 SN | 提示"无权发货：您不是该产品的当前货权方" |
| T503 | 重复发货 | 对在途 SN 再发一单 | 提示"该 SN 已有在途物流单" |
| T504 | 确认收货 | 用收货方账号登录 → 收货，输入运单号或 SN | 收货成功，`status = RECEIVED`，`receive_tx_hash` 非空，组装记录 `current_holder_id` 更新为收货方，状态变 `IN_STOCK` |
| T505 | SN 物流追踪 | 输入 SN → 追踪 | 时间线展示完整的发货→收货记录 |
| T506 | 批量发货 | 下载模板 → 填入多个 SN → 上传批量发货 | 逐个创建物流单 |
| T507 | 库存查看 | 库存管理页面 | 已收货的 SN 出现在库存列表中，可按状态筛选 |
| T508 | 登记销售 | 销售记录 → 登记销售，输入 SN、销售时间、客户姓名/手机、可选发票 | 销售成功，`bus_sales_record.tx_hash` 非空，`customer_hash` 为 SHA-256 |
| T509 | 非库存 SN 销售 | 输入一个不在库存中的 SN | 提示错误 |

---

### 阶段 6：终端用户 — 溯源 & 绑定 & 投诉 & 报废

| 编号 | 功能点 | 操作步骤 | 预期结果 |
|------|--------|---------|---------|
| T601 | 公开溯源查询 | 不登录，直接访问 `/trace`，输入已售出的 SN | 展示完整溯源信息：组装记录、部件 ECID 列表、物流记录、销售记录、质检报告 |
| T602 | 溯源-验证哈希 | 点击各"验证"按钮（设计文档、BOM、质检报告等） | 显示哈希一致/不一致 |
| T603 | 溯源-不存在的 SN | 输入随机 SN | 显示"未查询到该 SN 的装配记录"等警告 |
| T604 | 绑定产品 | `user01` 登录 → 产品绑定 → 输入 SN、销售时客户姓名、手机号 | 绑定成功，`bus_user_product.tx_hash` 非空 |
| T605 | 绑定-信息不一致 | 输入错误的姓名/手机 | 提示"姓名/手机号与销售登记信息不一致" |
| T606 | 重复绑定 | 再次绑定同一 SN | 返回已存在的绑定记录，不报错 |
| T607 | 发起投诉 | 投诉反馈 → 选择已绑定的 SN → 填写故障类型、描述 → 可上传证据照片 → 提交 | 投诉记录创建成功，列表可查 |
| T608 | 报废登记 | 报废登记 → 选择 SN → 填写回收商/处置方式 → 提交 | 报废记录创建，`tx_hash` 非空，产品状态变 `DECOMMISSIONED` |
| T609 | 已报废产品不可流转 | 尝试对已报废的 SN 发货 | 提示"该产品状态不允许再流转" |

---

### 阶段 7：监管 — 抽检 & 召回 & 异常监控

| 编号 | 功能点 | 操作步骤 | 预期结果 |
|------|--------|---------|---------|
| T701 | 创建抽检任务 | `reg01` 登录 → 抽检任务 → 新建，选择目标类型和 ID | 任务创建成功 |
| T702 | 提交抽检结果 | 对任务点"提交结果"，上传检测报告，选择"通过/不通过" | 结果提交成功，`tx_hash` 非空 |
| T703 | 召回分析 | 召回管理 → 输入有问题的 SN → 分析 | 展示组装记录、故障 ECID、故障批次 |
| T704 | 导出证据包(JSON) | 点击"导出证据包(JSON)" | 返回完整的 JSON 溯源数据 |
| T705 | 导出证据包(PDF) | 点击"导出证据包(PDF)" | 下载 PDF 文件，包含中文标题、产品信息表、部件溯源表、物流表、销售信息、页脚声明 |
| T706 | 发布召回通告 | 填写召回通告信息 → 提交 | 通告创建成功，列表可查 |
| T707 | 串货异常扫描 | 串货监控 → 扫描 | 展示最近 SN 的异常分析结果，标注风险等级 |
| T708 | 单 SN 异常分析 | 召回管理 → 输入 SN → 异常分析 | 返回物流链分析、绑定一致性等风险评估 |
| T709 | 自动召回调度器 | 查看调度器状态；点击"立即执行" | 调度器运行，返回状态信息 |

---

### 阶段 8：系统管理（admin / regulator）

| 编号 | 功能点 | 操作步骤 | 预期结果 |
|------|--------|---------|---------|
| T801 | 用户管理 | admin 登录 → 系统管理 → 用户管理 → 搜索/编辑/禁用/删除 | CRUD 操作正常 |
| T802 | 角色管理 | 角色管理 → 分配菜单 | 菜单树正确，保存后生效 |
| T803 | 菜单管理 | 新增/编辑/删除菜单 | CRUD 操作正常 |
| T804 | 操作日志 | 操作日志页面 → 按关键字搜索 | 日志列表可查、可翻页 |

---

### 阶段 9：区块链 & IPFS 基础设施验证

| 编号 | 功能点 | 操作步骤 | 预期结果 |
|------|--------|---------|---------|
| T901 | FISCO 健康检查 | 浏览器访问 `http://localhost:8088/api/debug/fisco/health` | 返回 `sdkAvailable: true`，`contractAddress` 非空 |
| T902 | 区块高度 | 访问 `/api/debug/fisco/block-number` | 返回 `blockNumber > 0`，`groupPeers` 列表非空 |
| T903 | Ping（发交易） | 登录后访问 `/api/debug/fisco/ping`（需 JWT） | 返回 `txHash`，链上可查 |
| T904 | 查询交易回执 | 用上一步的 txHash 访问 `/api/debug/fisco/tx/{txHash}` | 返回 `statusOk: true`，有 `blockNumber`、`from`、`to` 等 |
| T905 | IPFS 验证 | 在溯源页面对已上传的设计文档/BOM/质检报告点击"验证" | 从 IPFS 获取内容并与存储哈希比对一致 |

---

## 四、测试执行顺序

```
T001 → T002~T007 → T008 → T009~T010
  → T101~T103（监管审核）
  → T201~T207（供应商全流程）
  → T301~T313（制造商全流程）
  → T401~T410（组装商全流程）
  → T501~T509（分销商全流程）
  → T601~T609（终端用户全流程）
  → T701~T709（监管全流程）
  → T801~T804（系统管理）
  → T901~T905（基础设施验证）
```

> 按此顺序执行可以保证数据依赖关系正确：每个阶段的输出是下个阶段的输入（ECID → SN → 物流 → 销售 → 绑定）。

---

## 五、关键数据流验证 SQL

每完成一个阶段后，建议在数据库中执行以下检查：

### 1. 链上地址真实性

```sql
SELECT username, blockchain_addr
FROM sys_user
WHERE blockchain_addr LIKE '0x%' AND LENGTH(blockchain_addr) = 42;
```

### 2. 各表锚定覆盖率

```sql
SELECT 'production_request' AS t, COUNT(*) AS total, SUM(tx_hash IS NOT NULL) AS anchored FROM bus_production_request
UNION ALL
SELECT 'production_batch',      COUNT(*), SUM(tx_hash IS NOT NULL)       FROM bus_production_batch
UNION ALL
SELECT 'device_record',         COUNT(*), SUM(tx_hash IS NOT NULL)       FROM bus_device_record
UNION ALL
SELECT 'assembly_batch',        COUNT(*), SUM(tx_hash IS NOT NULL)       FROM bus_assembly_batch
UNION ALL
SELECT 'assembly_record',       COUNT(*), SUM(tx_hash IS NOT NULL)       FROM bus_assembly_record
UNION ALL
SELECT 'transfer_event',        COUNT(*), SUM(tx_hash IS NOT NULL)       FROM bus_transfer_event
UNION ALL
SELECT 'sales_record',          COUNT(*), SUM(tx_hash IS NOT NULL)       FROM bus_sales_record
UNION ALL
SELECT 'user_product',          COUNT(*), SUM(tx_hash IS NOT NULL)       FROM bus_user_product;
```

### 3. IPFS CID 有效性

```sql
SELECT id, ipfs_cid
FROM bus_design_document
WHERE ipfs_cid IS NOT NULL
LIMIT 5;
```

> 用返回的 CID 访问 `http://127.0.0.1:8080/ipfs/{cid}` 验证文件可下载。

### 4. 收货锚定覆盖

```sql
SELECT id, sn, status, tx_hash, receive_tx_hash
FROM bus_transfer_event
WHERE status = 'RECEIVED';
```

> `tx_hash`（发货锚定）和 `receive_tx_hash`（收货锚定）都应有值。

---

## 六、用例总数统计

| 阶段 | 用例数 |
|------|--------|
| 阶段 0：系统基础 & 注册 | 10 |
| 阶段 1：监管审核 | 3 |
| 阶段 2：供应商 | 7 |
| 阶段 3：制造商 | 13 |
| 阶段 4：组装商 | 10 |
| 阶段 5：分销商 | 9 |
| 阶段 6：终端用户 | 9 |
| 阶段 7：监管全流程 | 9 |
| 阶段 8：系统管理 | 4 |
| 阶段 9：基础设施 | 5 |
| **合计** | **79** |
